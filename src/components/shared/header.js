import React, { Component } from 'react';

class Header extends Component {
  render(){
    return(
      <header className="container">
        <h1 className="text-center">Posters App</h1>
      </header>
    );
  }
}

export default Header;
